<div class="section-title">

  <h1>teste </h1>

  <h4> <b> Você tem </b><%= count_new_messages %> <b> novas mensagens </b> </h4>

      <% @chats_user.each do |chat| %>


     <h4> <%= check_sender_or_receiver(chat).user_name %> </h4>
     <h4> <%= find_ad(chat.advertisement.id).destiny %> </h4>
     <h4> <%= find_ad(chat.advertisement.id).travel_date %> </h4>

     <li><%= link_to "Show",  "/chats/#{chat.id}" %></li>

      <% end %>
</div>
